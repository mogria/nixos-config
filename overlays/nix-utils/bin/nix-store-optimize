#! /usr/env/bin nix-shell
#! nix-shell -i bash -p coreutils -p pv

linebuf() {
    stdbuf -eL -oL "$@"
}

linebuf nix-store --optimise -vv 2>&1 | linebuf grep 'hashing files in' | linebuf cut -d/ -f4 | linebuf cut -b1 | linebuf uniq | pv -l -s 36 > /dev/null
